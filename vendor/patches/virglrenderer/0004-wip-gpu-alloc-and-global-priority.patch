diff --git a/server/render_context.c b/server/render_context.c
index 718b1b5..d58a6fb 100644
--- a/server/render_context.c
+++ b/server/render_context.c
@@ -62,8 +62,53 @@ render_context_dispatch_submit_fence(struct render_context *ctx,
    /* always merge fences */
    assert(!(req->flags & ~VIRGL_RENDERER_FENCE_FLAG_MERGEABLE));
    assert(req->ring_index < (uint32_t)ctx->timeline_count);
-   return render_state_submit_fence(ctx->ctx_id, VIRGL_RENDERER_FENCE_FLAG_MERGEABLE,
-                                    req->ring_index, req->seqno);
+   return render_state_submit_fence2(ctx->ctx_id, VIRGL_RENDERER_FENCE_FLAG_MERGEABLE,
+                                     req->ring_index, req->seqno,
+                                     req->timeline_point);
+}
+
+static bool
+render_context_dispatch_get_timeline(struct render_context *ctx,
+                                     UNUSED const union render_context_op_request *request,
+                                     UNUSED const int *fds,
+                                     UNUSED int fd_count)
+{
+   struct render_context_op_get_timeline_reply reply = {
+      .status = 1,
+   };
+
+   int out_fds[2] = { -1, -1 };
+   if (!render_state_get_timeline_fd(ctx->ctx_id, &out_fds[0], &out_fds[1])) {
+      reply.status = 0;
+      const bool ok = render_socket_send_reply_with_fds(&ctx->socket, &reply,
+                                                        sizeof(reply), out_fds, 2);
+      close(out_fds[0]);
+      close(out_fds[1]);
+      return ok;
+   }
+
+   return render_socket_send_reply(&ctx->socket, &reply, sizeof(reply));
+}
+
+static bool
+render_context_dispatch_import_semaphore(struct render_context *ctx,
+                                         const union render_context_op_request *request,
+                                         const int *fds,
+                                         int fd_count)
+{
+   const struct render_context_op_import_semaphore_request *req =
+      &request->import_semaphore;
+
+   struct render_context_op_import_semaphore_reply reply = {
+      .status = 1,
+   };
+   if (fd_count == 1 &&
+       !render_state_import_semaphore_fd(ctx->ctx_id, req->device_id,
+                                         req->semaphore_id, fds[0]))
+      reply.status = 0;
+
+   /* on success the driver owns the fd; on failure render_state closed it */
+   return render_socket_send_reply(&ctx->socket, &reply, sizeof(reply));
 }
 
 static bool
@@ -221,6 +266,8 @@ static const struct render_context_dispatch_entry
       RENDER_CONTEXT_DISPATCH(DESTROY_RESOURCE, destroy_resource, 0),
       RENDER_CONTEXT_DISPATCH(SUBMIT_CMD, submit_cmd, 0),
       RENDER_CONTEXT_DISPATCH(SUBMIT_FENCE, submit_fence, 0),
+      RENDER_CONTEXT_DISPATCH(GET_TIMELINE, get_timeline, 0),
+      RENDER_CONTEXT_DISPATCH(IMPORT_SEMAPHORE, import_semaphore, 1),
 #undef RENDER_CONTEXT_DISPATCH
    };
 
diff --git a/server/render_protocol.h b/server/render_protocol.h
index ed74a1e..a172b75 100644
--- a/server/render_protocol.h
+++ b/server/render_protocol.h
@@ -40,6 +40,8 @@ enum render_context_op {
    RENDER_CONTEXT_OP_SUBMIT_CMD,
    RENDER_CONTEXT_OP_SUBMIT_FENCE,
    RENDER_CONTEXT_OP_EXPORT_FENCE,
+   RENDER_CONTEXT_OP_GET_TIMELINE,
+   RENDER_CONTEXT_OP_IMPORT_SEMAPHORE,
 
    RENDER_CONTEXT_OP_COUNT,
 };
@@ -207,6 +209,11 @@ struct render_context_op_submit_fence_request {
    /* TODO fix virgl_renderer_context_create_fence to use ring_index */
    uint32_t ring_index;
    uint32_t seqno;
+   uint32_t pad;
+   /* when non-zero, materialize the fence into the context's shared DRM
+    * timeline syncobj at this point (see RENDER_CONTEXT_OP_GET_TIMELINE)
+    */
+   uint64_t timeline_point;
 };
 
 /* Export a sync_file fd for a previously submitted, not-yet-retired fence.
@@ -236,6 +243,33 @@ struct render_context_op_export_fence_reply {
    /* followed by 1 fd if status == RENDER_CONTEXT_EXPORT_FENCE_OK */
 };
 
+/* Get an fd of the context's DRM timeline syncobj, creating the timeline on
+ * first use.  Points on the timeline are materialized by submit_fence
+ * requests carrying a non-zero timeline_point.
+ */
+struct render_context_op_get_timeline_request {
+   struct render_context_op_header header;
+};
+
+struct render_context_op_get_timeline_reply {
+   uint32_t status; /* 0 = ok, followed by 1 syncobj fd; non-zero = error */
+};
+
+/* Import a sync_file fd (carried with the request) as the TEMPORARY payload
+ * of a venus binary semaphore, so a subsequent queue submission waits it on
+ * the GPU instead of the guest CPU-waiting the fd before submitting.
+ * The reply provides the ordering barrier.
+ */
+struct render_context_op_import_semaphore_request {
+   struct render_context_op_header header;
+   uint64_t device_id;    /* venus object id of the VkDevice */
+   uint64_t semaphore_id; /* venus object id of the VkSemaphore */
+};
+
+struct render_context_op_import_semaphore_reply {
+   uint32_t status; /* 0 = ok */
+};
+
 union render_context_op_request {
    struct render_context_op_header header;
    struct render_context_op_nop_request nop;
@@ -246,6 +280,8 @@ union render_context_op_request {
    struct render_context_op_submit_cmd_request submit_cmd;
    struct render_context_op_submit_fence_request submit_fence;
    struct render_context_op_export_fence_request export_fence;
+   struct render_context_op_get_timeline_request get_timeline;
+   struct render_context_op_import_semaphore_request import_semaphore;
 };
 
 #ifdef __APPLE__
diff --git a/server/render_state.c b/server/render_state.c
index e90a289..58b1545 100644
--- a/server/render_state.c
+++ b/server/render_state.c
@@ -224,6 +224,35 @@ render_state_export_fence(uint32_t ctx_id, uint32_t ring_idx, uint64_t fence_id,
    return vkr_renderer_export_fence(ctx_id, ring_idx, fence_id, out_fd);
 }
 
+bool
+render_state_submit_fence2(uint32_t ctx_id,
+                           uint32_t flags,
+                           uint64_t ring_idx,
+                           uint64_t fence_id,
+                           uint64_t timeline_point)
+{
+   SCOPE_LOCK_RENDERER();
+   return vkr_renderer_submit_fence2(ctx_id, flags, ring_idx, fence_id, timeline_point);
+}
+
+int
+render_state_get_timeline_fd(uint32_t ctx_id, int *out_syncobj_fd, int *out_node_fd)
+{
+   SCOPE_LOCK_RENDERER();
+   return vkr_renderer_get_timeline_fd(ctx_id, out_syncobj_fd, out_node_fd);
+}
+
+int
+render_state_import_semaphore_fd(uint32_t ctx_id,
+                                 uint64_t device_id,
+                                 uint64_t semaphore_id,
+                                 int sync_file_fd)
+{
+   SCOPE_LOCK_RENDERER();
+   return vkr_renderer_import_semaphore_sync_fd(ctx_id, device_id, semaphore_id,
+                                                sync_file_fd);
+}
+
 bool
 render_state_create_resource(uint32_t ctx_id,
                              uint32_t res_id,
diff --git a/server/render_state.h b/server/render_state.h
index e271986..819f4e3 100644
--- a/server/render_state.h
+++ b/server/render_state.h
@@ -36,6 +36,26 @@ render_state_submit_fence(uint32_t ctx_id,
 int
 render_state_export_fence(uint32_t ctx_id, uint32_t ring_idx, uint64_t fence_id, int *out_fd);
 
+bool
+render_state_submit_fence2(uint32_t ctx_id,
+                           uint32_t flags,
+                           uint64_t ring_idx,
+                           uint64_t fence_id,
+                           uint64_t timeline_point);
+
+/* returns 0 and caller-owned fds (timeline syncobj + render node), or
+ * negative errno
+ */
+int
+render_state_get_timeline_fd(uint32_t ctx_id, int *out_syncobj_fd, int *out_node_fd);
+
+/* consumes sync_file_fd (driver owns it on success, closed on failure) */
+int
+render_state_import_semaphore_fd(uint32_t ctx_id,
+                                 uint64_t device_id,
+                                 uint64_t semaphore_id,
+                                 int sync_file_fd);
+
 bool
 render_state_create_resource(uint32_t ctx_id,
                              uint32_t res_id,
diff --git a/src/proxy/proxy_context.c b/src/proxy/proxy_context.c
index b718ef2..1a5c533 100644
--- a/src/proxy/proxy_context.c
+++ b/src/proxy/proxy_context.c
@@ -291,10 +291,11 @@ proxy_context_export_fence(struct virgl_context *base,
 }
 
 static int
-proxy_context_submit_fence(struct virgl_context *base,
-                           uint32_t flags,
-                           uint32_t ring_idx,
-                           uint64_t fence_id)
+proxy_context_submit_fence2(struct virgl_context *base,
+                            uint32_t flags,
+                            uint32_t ring_idx,
+                            uint64_t fence_id,
+                            uint64_t timeline_point)
 {
    struct proxy_context *ctx = (struct proxy_context *)base;
    const uint64_t old_busy_mask = ctx->timeline_busy_mask;
@@ -325,6 +326,7 @@ proxy_context_submit_fence(struct virgl_context *base,
       .flags = flags,
       .ring_index = ring_idx,
       .seqno = fence->seqno,
+      .timeline_point = timeline_point,
    };
    if (proxy_socket_send_request(&ctx->socket, &req, sizeof(req)))
       return 0;
@@ -344,6 +346,81 @@ proxy_context_submit_fence(struct virgl_context *base,
    return -1;
 }
 
+static int
+proxy_context_submit_fence(struct virgl_context *base,
+                           uint32_t flags,
+                           uint32_t ring_idx,
+                           uint64_t fence_id)
+{
+   return proxy_context_submit_fence2(base, flags, ring_idx, fence_id, 0);
+}
+
+static int
+proxy_context_get_timeline_fd(struct virgl_context *base,
+                              int *out_syncobj_fd,
+                              int *out_node_fd)
+{
+   struct proxy_context *ctx = (struct proxy_context *)base;
+
+   const struct render_context_op_get_timeline_request req = {
+      .header.op = RENDER_CONTEXT_OP_GET_TIMELINE,
+   };
+   if (!proxy_socket_send_request(&ctx->socket, &req, sizeof(req))) {
+      proxy_log("failed to send get_timeline");
+      return -ENOTCONN;
+   }
+
+   struct render_context_op_get_timeline_reply reply;
+   int reply_fds[2] = { -1, -1 };
+   int reply_fd_count = 0;
+   if (!proxy_socket_receive_reply_with_fds(&ctx->socket, &reply, sizeof(reply),
+                                            reply_fds, 2, &reply_fd_count)) {
+      proxy_log("failed to get get_timeline reply");
+      return -ENOTCONN;
+   }
+
+   if (reply.status || reply_fd_count != 2) {
+      for (int i = 0; i < reply_fd_count; i++)
+         close(reply_fds[i]);
+      return -ENODEV;
+   }
+
+   *out_syncobj_fd = reply_fds[0];
+   *out_node_fd = reply_fds[1];
+   return 0;
+}
+
+static int
+proxy_context_import_semaphore_fd(struct virgl_context *base,
+                                  uint64_t device_id,
+                                  uint64_t semaphore_id,
+                                  int sync_file_fd)
+{
+   struct proxy_context *ctx = (struct proxy_context *)base;
+
+   const struct render_context_op_import_semaphore_request req = {
+      .header.op = RENDER_CONTEXT_OP_IMPORT_SEMAPHORE,
+      .device_id = device_id,
+      .semaphore_id = semaphore_id,
+   };
+   if (!proxy_socket_send_request_with_fds(&ctx->socket, &req, sizeof(req),
+                                           &sync_file_fd, 1)) {
+      proxy_log("failed to send import_semaphore");
+      close(sync_file_fd);
+      return -ENOTCONN;
+   }
+   /* the fd has been duplicated into the request; we still own our copy */
+   close(sync_file_fd);
+
+   struct render_context_op_import_semaphore_reply reply;
+   if (!proxy_socket_receive_reply(&ctx->socket, &reply, sizeof(reply))) {
+      proxy_log("failed to get import_semaphore reply");
+      return -ENOTCONN;
+   }
+
+   return reply.status ? -EINVAL : 0;
+}
+
 static void
 proxy_context_retire_fences(struct virgl_context *base)
 {
@@ -660,6 +737,9 @@ proxy_context_init_base(struct proxy_context *ctx)
    ctx->base.retire_fences = proxy_context_retire_fences;
    ctx->base.submit_fence = proxy_context_submit_fence;
    ctx->base.export_fence = proxy_context_export_fence;
+   ctx->base.submit_fence2 = proxy_context_submit_fence2;
+   ctx->base.get_timeline_fd = proxy_context_get_timeline_fd;
+   ctx->base.import_semaphore_fd = proxy_context_import_semaphore_fd;
 }
 
 static bool
diff --git a/src/venus/vkr_context.c b/src/venus/vkr_context.c
index c170bd3..9b8992c 100644
--- a/src/venus/vkr_context.c
+++ b/src/venus/vkr_context.c
@@ -5,10 +5,19 @@
 
 #include "vkr_context.h"
 
+#include <fcntl.h>
+#include <stdio.h>
+#include <sys/ioctl.h>
 #include <sys/mman.h>
 #include <sys/types.h>
 #include <unistd.h>
 
+/* the bundled UAPI header (src/drm/drm-uapi) — NOT <drm/drm.h>: the one-ioctl
+ * timeline import needs the kernel >= 6.16 syncobj UAPI (FD_TO_HANDLE
+ * FLAGS_TIMELINE + drm_syncobj_handle.point), which older distro headers
+ * lack; older kernels take the runtime EINVAL fallback instead. */
+#include "drm.h"
+
 #include "util/anon_file.h"
 #include "venus-protocol/vn_protocol_renderer_dispatches.h"
 
@@ -136,10 +145,11 @@ vkr_context_init_proc_table(struct vkr_context *ctx)
 }
 
 bool
-vkr_context_submit_fence(struct vkr_context *ctx,
-                         uint32_t flags,
-                         uint32_t ring_idx,
-                         uint64_t fence_id)
+vkr_context_submit_fence2(struct vkr_context *ctx,
+                          uint32_t flags,
+                          uint32_t ring_idx,
+                          uint64_t fence_id,
+                          uint64_t timeline_point)
 {
    /* retire fence on cpu timeline directly */
    if (ring_idx == 0) {
@@ -155,11 +165,309 @@ vkr_context_submit_fence(struct vkr_context *ctx,
    /* always merge fences */
    assert(!(flags & ~VIRGL_RENDERER_FENCE_FLAG_MERGEABLE));
    flags = VIRGL_RENDERER_FENCE_FLAG_MERGEABLE;
-   bool ok = vkr_queue_sync_submit(ctx->sync_queues[ring_idx], flags, ring_idx, fence_id);
+   bool ok = vkr_queue_sync_submit(ctx->sync_queues[ring_idx], flags, ring_idx,
+                                   fence_id, timeline_point);
 
    return ok;
 }
 
+bool
+vkr_context_submit_fence(struct vkr_context *ctx,
+                         uint32_t flags,
+                         uint32_t ring_idx,
+                         uint64_t fence_id)
+{
+   return vkr_context_submit_fence2(ctx, flags, ring_idx, fence_id, 0);
+}
+
+static int64_t
+vkr_timeline_now_ns(void)
+{
+   struct timespec ts;
+   clock_gettime(CLOCK_MONOTONIC, &ts);
+   return (int64_t)ts.tv_sec * 1000000000 + ts.tv_nsec;
+}
+
+/* Perform one sync_file -> timeline-point import.  Runs on the timeline
+ * worker (or inline if the worker failed to start); ctx->timeline handles
+ * are immutable once created, so no lock is needed here.
+ */
+static void
+vkr_context_timeline_import_one(struct vkr_context *ctx,
+                                int sync_file_fd,
+                                uint64_t timeline_point)
+{
+   const int drm_fd = ctx->timeline.drm_fd;
+   bool imported = false;
+
+   const int64_t t0 = vkr_timeline_now_ns();
+   int64_t t_ioctl = t0, t_close = t0;
+
+   if (sync_file_fd >= 0 && ctx->timeline.one_ioctl) {
+      /* one-ioctl chain-add (kernel >= 6.16): no fence replacement happens,
+       * so no prior NVIDIA fence is released synchronously in the ioctl;
+       * waiters wake on the new point before the chain GC walk runs
+       */
+      struct drm_syncobj_handle import = {
+         .handle = ctx->timeline.handle,
+         .flags = DRM_SYNCOBJ_FD_TO_HANDLE_FLAGS_IMPORT_SYNC_FILE |
+                  DRM_SYNCOBJ_FD_TO_HANDLE_FLAGS_TIMELINE,
+         .fd = sync_file_fd,
+         .point = timeline_point,
+      };
+      if (!ioctl(drm_fd, DRM_IOCTL_SYNCOBJ_FD_TO_HANDLE, &import)) {
+         imported = true;
+      } else if (errno == EINVAL) {
+         vkr_log("timeline: no one-ioctl timeline import (pre-6.16 kernel), "
+                 "using scratch-syncobj fallback");
+         ctx->timeline.one_ioctl = false;
+      }
+   }
+
+   if (sync_file_fd >= 0 && !imported && !ctx->timeline.one_ioctl) {
+      /* two-step fallback via the persistent scratch binary syncobj:
+       * IMPORT_SYNC_FILE replaces its fence, then transfer to the point
+       */
+      struct drm_syncobj_handle import = {
+         .handle = ctx->timeline.scratch,
+         .flags = DRM_SYNCOBJ_FD_TO_HANDLE_FLAGS_IMPORT_SYNC_FILE,
+         .fd = sync_file_fd,
+      };
+      struct drm_syncobj_transfer xfer = {
+         .src_handle = ctx->timeline.scratch,
+         .dst_handle = ctx->timeline.handle,
+         .dst_point = timeline_point,
+      };
+      imported = !ioctl(drm_fd, DRM_IOCTL_SYNCOBJ_FD_TO_HANDLE, &import) &&
+                 !ioctl(drm_fd, DRM_IOCTL_SYNCOBJ_TRANSFER, &xfer);
+   }
+
+   if (!imported) {
+      /* fd < 0 = fence already signaled; also the failure fallback so the
+       * client never stalls on WAIT_AVAILABLE
+       */
+      if (sync_file_fd >= 0)
+         vkr_log("timeline: import at point %" PRIu64 " failed, signaling",
+                 timeline_point);
+      const uint64_t point = timeline_point;
+      const uint32_t handle = ctx->timeline.handle;
+      struct drm_syncobj_timeline_array signal = {
+         .handles = (uintptr_t)&handle,
+         .points = (uintptr_t)&point,
+         .count_handles = 1,
+      };
+      ioctl(drm_fd, DRM_IOCTL_SYNCOBJ_TIMELINE_SIGNAL, &signal);
+   }
+
+   t_ioctl = vkr_timeline_now_ns();
+   if (sync_file_fd >= 0)
+      close(sync_file_fd);
+   t_close = vkr_timeline_now_ns();
+
+   if (t_close - t0 > 2000000)
+      vkr_log("NVWD-TL slow import point %" PRIu64 ": ioctls %" PRId64
+              " us close %" PRId64 " us (%s)",
+              timeline_point, (t_ioctl - t0) / 1000, (t_close - t_ioctl) / 1000,
+              ctx->timeline.one_ioctl ? "one-ioctl" : "scratch");
+}
+
+static int
+vkr_context_timeline_worker(void *arg)
+{
+   struct vkr_context *ctx = arg;
+
+   mtx_lock(&ctx->timeline.mutex);
+   while (true) {
+      while (!ctx->timeline.worker.count && !ctx->timeline.worker.join)
+         cnd_wait(&ctx->timeline.worker.cond, &ctx->timeline.mutex);
+
+      struct {
+         int fd;
+         uint64_t point;
+      } jobs[ARRAY_SIZE(ctx->timeline.worker.jobs)];
+      const uint32_t count = ctx->timeline.worker.count;
+      memcpy(jobs, ctx->timeline.worker.jobs, sizeof(jobs[0]) * count);
+      ctx->timeline.worker.count = 0;
+      const bool join = ctx->timeline.worker.join;
+      cnd_broadcast(&ctx->timeline.worker.space);
+      mtx_unlock(&ctx->timeline.mutex);
+
+      for (uint32_t i = 0; i < count; i++)
+         vkr_context_timeline_import_one(ctx, jobs[i].fd, jobs[i].point);
+
+      if (join)
+         return 0;
+      mtx_lock(&ctx->timeline.mutex);
+   }
+}
+
+/* Any render node works: syncobjs are containers for driver-agnostic
+ * dma_fences and all syncobj ioctls are DRM_RENDER_ALLOW.  drm_fd == -2
+ * marks a failed earlier attempt.
+ */
+static bool
+vkr_context_timeline_ensure_locked(struct vkr_context *ctx)
+{
+   if (ctx->timeline.handle)
+      return true;
+   if (ctx->timeline.drm_fd == -2)
+      return false;
+
+   for (int minor = 128; minor < 128 + 16; minor++) {
+      char path[64];
+      snprintf(path, sizeof(path), "/dev/dri/renderD%d", minor);
+      int fd = open(path, O_RDWR | O_CLOEXEC);
+      if (fd < 0)
+         continue;
+      snprintf(ctx->timeline.node_path, sizeof(ctx->timeline.node_path), "%s", path);
+
+      struct drm_get_cap cap = { .capability = DRM_CAP_SYNCOBJ_TIMELINE };
+      struct drm_syncobj_create create = { 0 };
+      struct drm_syncobj_create scratch = { 0 };
+      if (ioctl(fd, DRM_IOCTL_GET_CAP, &cap) || !cap.value ||
+          ioctl(fd, DRM_IOCTL_SYNCOBJ_CREATE, &create) ||
+          ioctl(fd, DRM_IOCTL_SYNCOBJ_CREATE, &scratch)) {
+         close(fd);
+         continue;
+      }
+
+      ctx->timeline.drm_fd = fd;
+      ctx->timeline.handle = create.handle;
+      ctx->timeline.scratch = scratch.handle;
+      ctx->timeline.one_ioctl = true;
+
+      if (cnd_init(&ctx->timeline.worker.cond) == thrd_success) {
+         if (cnd_init(&ctx->timeline.worker.space) == thrd_success) {
+            if (thrd_create(&ctx->timeline.worker.thread,
+                            vkr_context_timeline_worker, ctx) == thrd_success) {
+               ctx->timeline.worker.started = true;
+            } else {
+               cnd_destroy(&ctx->timeline.worker.space);
+               cnd_destroy(&ctx->timeline.worker.cond);
+            }
+         } else {
+            cnd_destroy(&ctx->timeline.worker.cond);
+         }
+      }
+      return true;
+   }
+
+   vkr_log("timeline: no syncobj-timeline-capable render node found");
+   ctx->timeline.drm_fd = -2;
+   return false;
+}
+
+int
+vkr_context_get_timeline_fd(struct vkr_context *ctx,
+                            int *out_syncobj_fd,
+                            int *out_node_fd)
+{
+   mtx_lock(&ctx->timeline.mutex);
+   if (!vkr_context_timeline_ensure_locked(ctx)) {
+      mtx_unlock(&ctx->timeline.mutex);
+      return -ENODEV;
+   }
+
+   /* the client also gets a render-node fd so it can perform syncobj ioctls
+    * without needing (or having permissions on) a /dev/dri node of its own —
+    * fd passing sidesteps path access entirely.
+    *
+    * This must be a FRESH open, not a dup of our fd: a dup shares the
+    * struct file (and thus the drm_file), and DRM core's
+    * drm_file_update_pid() runs mutex + synchronize_rcu (~10 ms under load)
+    * on EVERY ioctl whose caller tgid differs from the previous one — two
+    * processes alternating ioctls on one drm_file poisons both.  With a
+    * fresh file the client is its sole user after a single handover.
+    */
+   struct drm_syncobj_handle args = { .handle = ctx->timeline.handle };
+   const int ret = ioctl(ctx->timeline.drm_fd, DRM_IOCTL_SYNCOBJ_HANDLE_TO_FD, &args);
+   if (ret) {
+      mtx_unlock(&ctx->timeline.mutex);
+      return -errno;
+   }
+
+   const int node_fd = open(ctx->timeline.node_path, O_RDWR | O_CLOEXEC);
+   mtx_unlock(&ctx->timeline.mutex);
+   if (node_fd < 0) {
+      close(args.fd);
+      return -errno;
+   }
+
+   *out_syncobj_fd = args.fd;
+   *out_node_fd = node_fd;
+   return 0;
+}
+
+void
+vkr_context_timeline_import(struct vkr_context *ctx,
+                            int sync_file_fd,
+                            uint64_t timeline_point)
+{
+   mtx_lock(&ctx->timeline.mutex);
+
+   /* nothing to do until a client has fetched the timeline */
+   if (!ctx->timeline.handle) {
+      mtx_unlock(&ctx->timeline.mutex);
+      if (sync_file_fd >= 0)
+         close(sync_file_fd);
+      return;
+   }
+
+   if (ctx->timeline.worker.started) {
+      /* full queue = 64 frames of backlog; block rather than reorder (points
+       * must land monotonically) or import inline (would bill fence releases
+       * to this thread)
+       */
+      while (ctx->timeline.worker.count == ARRAY_SIZE(ctx->timeline.worker.jobs))
+         cnd_wait(&ctx->timeline.worker.space, &ctx->timeline.mutex);
+
+      ctx->timeline.worker.jobs[ctx->timeline.worker.count].fd = sync_file_fd;
+      ctx->timeline.worker.jobs[ctx->timeline.worker.count].point = timeline_point;
+      ctx->timeline.worker.count++;
+      cnd_signal(&ctx->timeline.worker.cond);
+      mtx_unlock(&ctx->timeline.mutex);
+      return;
+   }
+
+   mtx_unlock(&ctx->timeline.mutex);
+   vkr_context_timeline_import_one(ctx, sync_file_fd, timeline_point);
+}
+
+int
+vkr_context_import_semaphore_sync_fd(struct vkr_context *ctx,
+                                     uint64_t device_id,
+                                     uint64_t semaphore_id,
+                                     int sync_file_fd)
+{
+   struct vkr_device *dev = vkr_context_get_object(ctx, device_id);
+   struct vkr_semaphore *sem = vkr_context_get_object(ctx, semaphore_id);
+   if (!dev || dev->base.type != VK_OBJECT_TYPE_DEVICE || !sem ||
+       sem->base.type != VK_OBJECT_TYPE_SEMAPHORE) {
+      vkr_log("import_semaphore: bad object ids dev %" PRIu64 " sem %" PRIu64,
+              device_id, semaphore_id);
+      close(sync_file_fd);
+      return -EINVAL;
+   }
+
+   const VkImportSemaphoreFdInfoKHR info = {
+      .sType = VK_STRUCTURE_TYPE_IMPORT_SEMAPHORE_FD_INFO_KHR,
+      .semaphore = sem->base.handle.semaphore,
+      .flags = VK_SEMAPHORE_IMPORT_TEMPORARY_BIT,
+      .handleType = VK_EXTERNAL_SEMAPHORE_HANDLE_TYPE_SYNC_FD_BIT,
+      .fd = sync_file_fd,
+   };
+   VkResult result =
+      dev->proc_table.ImportSemaphoreFdKHR(dev->base.handle.device, &info);
+   if (result != VK_SUCCESS) {
+      vkr_log("import_semaphore: vkImportSemaphoreFdKHR failed %d", result);
+      close(sync_file_fd);
+      return -EINVAL;
+   }
+
+   /* success: the driver owns the fd */
+   return 0;
+}
+
 int
 vkr_context_export_fence(struct vkr_context *ctx,
                          uint32_t ring_idx,
@@ -644,6 +952,25 @@ vkr_context_destroy(struct vkr_context *ctx)
 
    vkr_context_wait_ring_fini(ctx);
 
+   if (ctx->timeline.worker.started) {
+      mtx_lock(&ctx->timeline.mutex);
+      ctx->timeline.worker.join = true;
+      cnd_signal(&ctx->timeline.worker.cond);
+      mtx_unlock(&ctx->timeline.mutex);
+      thrd_join(ctx->timeline.worker.thread, NULL);
+      cnd_destroy(&ctx->timeline.worker.space);
+      cnd_destroy(&ctx->timeline.worker.cond);
+   }
+   if (ctx->timeline.handle) {
+      struct drm_syncobj_destroy destroy = { .handle = ctx->timeline.handle };
+      ioctl(ctx->timeline.drm_fd, DRM_IOCTL_SYNCOBJ_DESTROY, &destroy);
+      destroy.handle = ctx->timeline.scratch;
+      ioctl(ctx->timeline.drm_fd, DRM_IOCTL_SYNCOBJ_DESTROY, &destroy);
+   }
+   if (ctx->timeline.drm_fd >= 0)
+      close(ctx->timeline.drm_fd);
+   mtx_destroy(&ctx->timeline.mutex);
+
    if (ctx->ring_monitor.started)
       vkr_context_ring_monitor_fini(ctx);
 
@@ -727,6 +1054,11 @@ vkr_context_create(uint32_t ctx_id,
    if (!vkr_context_wait_ring_init(ctx))
       goto err_ctx_wait_ring_init;
 
+   if (mtx_init(&ctx->timeline.mutex, mtx_plain) != thrd_success)
+      goto err_ctx_wait_ring_init;
+   ctx->timeline.drm_fd = -1;
+   ctx->timeline.handle = 0;
+
    if (mtx_init(&ctx->object_mutex, mtx_plain) != thrd_success)
       goto err_ctx_object_mutex;
 
diff --git a/src/venus/vkr_context.h b/src/venus/vkr_context.h
index 7ba72eb..ce1a850 100644
--- a/src/venus/vkr_context.h
+++ b/src/venus/vkr_context.h
@@ -98,6 +98,50 @@ struct vkr_context {
 
    struct vkr_queue *sync_queues[64];
 
+   /* Shared DRM timeline syncobj: fences submitted with a timeline_point are
+    * materialized (GetFenceFdKHR + import) at that point right after
+    * QueueSubmit, so a client sharing the syncobj can export per-point
+    * sync_files locally.  Lazily created on first get_timeline_fd.
+    */
+   struct {
+      mtx_t mutex;
+      int drm_fd;      /* render node used for syncobj ioctls, -1 = none */
+      char node_path[64]; /* path of drm_fd, for fresh per-client opens */
+      uint32_t handle; /* timeline syncobj handle, 0 = not created */
+      /* scratch binary syncobj for the two-step import fallback
+       * (IMPORT_SYNC_FILE + TRANSFER) on kernels without the one-ioctl
+       * timeline import
+       */
+      uint32_t scratch;
+      /* one-ioctl sync_file -> timeline-point import,
+       * DRM_SYNCOBJ_FD_TO_HANDLE_FLAGS_IMPORT_SYNC_FILE | _TIMELINE
+       * (kernel >= 6.16); cleared on EINVAL and the scratch fallback takes
+       * over.  Only the import path (single thread) mutates this.
+       */
+      bool one_ioctl;
+
+      /* All dma_fence-release-bearing work runs on this worker, off the
+       * dispatch thread: the chain-add import garbage-collects prior chain
+       * nodes (running the driver's fence release callbacks, milliseconds
+       * on NVIDIA under concurrent submits), and closing a sync_file defers
+       * its teardown via task_work to the closing thread's next syscall
+       * exit.  Jobs are FIFO on a single thread so timeline points land in
+       * submit order, keeping dma_fence_chain seqnos monotonic.
+       */
+      struct {
+         thrd_t thread;
+         cnd_t cond;  /* worker wake: jobs pending or join */
+         cnd_t space; /* producer wake: queue no longer full */
+         bool started;
+         bool join;
+         struct {
+            int fd; /* sync_file, or -1 to signal the point directly */
+            uint64_t point;
+         } jobs[64];
+         uint32_t count;
+      } worker;
+   } timeline;
+
    struct vkr_instance *instance;
    char *instance_name;
 
@@ -130,6 +174,40 @@ vkr_context_submit_fence(struct vkr_context *ctx,
                          uint32_t ring_idx,
                          uint64_t fence_id);
 
+/* like vkr_context_submit_fence; timeline_point != 0 also materializes the
+ * fence into ctx->timeline at that point
+ */
+bool
+vkr_context_submit_fence2(struct vkr_context *ctx,
+                          uint32_t flags,
+                          uint32_t ring_idx,
+                          uint64_t fence_id,
+                          uint64_t timeline_point);
+
+/* returns 0 and caller-owned fds (timeline syncobj + render node for syncobj
+ * ioctls), or negative errno
+ */
+int
+vkr_context_get_timeline_fd(struct vkr_context *ctx,
+                            int *out_syncobj_fd,
+                            int *out_node_fd);
+
+/* consumes sync_file_fd; called by vkr_queue with a materialized fence fd */
+void
+vkr_context_timeline_import(struct vkr_context *ctx,
+                            int sync_file_fd,
+                            uint64_t timeline_point);
+
+/* import a sync_file as the TEMPORARY payload of a venus binary semaphore
+ * (guest passed the fd over the socket so the GPU waits it instead of the
+ * guest CPU); consumes sync_file_fd
+ */
+int
+vkr_context_import_semaphore_sync_fd(struct vkr_context *ctx,
+                                     uint64_t device_id,
+                                     uint64_t semaphore_id,
+                                     int sync_file_fd);
+
 bool
 vkr_context_submit_cmd(struct vkr_context *ctx, const void *buffer, size_t size);
 
diff --git a/src/venus/vkr_device.c b/src/venus/vkr_device.c
index 3e943bf..3880c3e 100644
--- a/src/venus/vkr_device.c
+++ b/src/venus/vkr_device.c
@@ -108,6 +108,8 @@ vkr_dispatch_vkCreateDevice(struct vn_dispatch_context *dispatch,
          const uint32_t queue_count = args->pCreateInfo->pQueueCreateInfos[i].queueCount;
 
          if (queue_family_index >= physical_dev->queue_family_property_count) {
+            vkr_log("CreateDevice: bad queueFamilyIndex %u (families %u)",
+                    queue_family_index, physical_dev->queue_family_property_count);
             args->ret = VK_ERROR_UNKNOWN;
             break;
          }
@@ -117,6 +119,9 @@ vkr_dispatch_vkCreateDevice(struct vn_dispatch_context *dispatch,
 
          if (queue_family_properties->queueCount - counts[queue_family_index] <
              queue_count) {
+            vkr_log("CreateDevice: family %u queueCount %u exceeds available %u",
+                    queue_family_index, queue_count,
+                    queue_family_properties->queueCount - counts[queue_family_index]);
             args->ret = VK_ERROR_UNKNOWN;
             break;
          }
@@ -176,7 +181,38 @@ vkr_dispatch_vkCreateDevice(struct vn_dispatch_context *dispatch,
    vn_replace_vkCreateDevice_args_handle(args);
    args->ret = vk->CreateDevice(args->physicalDevice, args->pCreateInfo, NULL,
                                 &dev->base.handle.device);
+
+   /* Android clients (HWUI) request a global queue priority and expect it to
+    * be honored best-effort, but desktop drivers (NVIDIA) reject unprivileged
+    * priority requests with NOT_PERMITTED. Retry without the priority chain.
+    */
+   if (args->ret == VK_ERROR_NOT_PERMITTED_KHR) {
+      bool stripped = false;
+      for (uint32_t i = 0; i < args->pCreateInfo->queueCreateInfoCount; i++) {
+         VkDeviceQueueCreateInfo *qci =
+            (VkDeviceQueueCreateInfo *)&args->pCreateInfo->pQueueCreateInfos[i];
+         VkBaseOutStructure *prev = (VkBaseOutStructure *)qci;
+         for (VkBaseOutStructure *s = prev->pNext; s; s = s->pNext) {
+            if (s->sType ==
+                VK_STRUCTURE_TYPE_DEVICE_QUEUE_GLOBAL_PRIORITY_CREATE_INFO_KHR) {
+               prev->pNext = s->pNext;
+               stripped = true;
+            } else {
+               prev = s;
+            }
+         }
+      }
+      if (stripped) {
+         vkr_log("CreateDevice: NOT_PERMITTED; retrying without global queue "
+                 "priority");
+         args->ret = vk->CreateDevice(args->physicalDevice, args->pCreateInfo,
+                                      NULL, &dev->base.handle.device);
+      }
+   }
+
    if (args->ret != VK_SUCCESS) {
+      vkr_log("CreateDevice: host driver returned %d (extCount=%u)", args->ret,
+              args->pCreateInfo->enabledExtensionCount);
       free(exts);
       free(dev);
       return;
diff --git a/src/venus/vkr_queue.c b/src/venus/vkr_queue.c
index b7242e0..4287de8 100644
--- a/src/venus/vkr_queue.c
+++ b/src/venus/vkr_queue.c
@@ -79,7 +79,8 @@ bool
 vkr_queue_sync_submit(struct vkr_queue *queue,
                       uint32_t flags,
                       uint32_t ring_idx,
-                      uint64_t fence_id)
+                      uint64_t fence_id,
+                      uint64_t timeline_point)
 {
    TRACE_FUNC();
    struct vkr_device *dev = queue->device;
@@ -103,6 +104,26 @@ vkr_queue_sync_submit(struct vkr_queue *queue,
       return false;
    }
 
+   /* Materialize the fresh fence into the context's shared timeline: SYNC_FD
+    * export has copy transference and the fence has a pending signal op, so
+    * exporting right after QueueSubmit is spec-legal.  fd == -1 (already
+    * signaled) and errors both make vkr_context_timeline_import signal the
+    * point directly.
+    */
+   if (timeline_point) {
+      int fd = -1;
+      if (dev->physical_device->KHR_external_fence_fd && !sync->device_lost) {
+         const VkFenceGetFdInfoKHR info = {
+            .sType = VK_STRUCTURE_TYPE_FENCE_GET_FD_INFO_KHR,
+            .fence = sync->fence,
+            .handleType = VK_EXTERNAL_FENCE_HANDLE_TYPE_SYNC_FD_BIT,
+         };
+         if (vk->GetFenceFdKHR(dev->base.handle.device, &info, &fd) != VK_SUCCESS)
+            fd = -1;
+      }
+      vkr_context_timeline_import(queue->context, fd, timeline_point);
+   }
+
    mtx_lock(&queue->sync_thread.mutex);
    list_addtail(&sync->head, &queue->sync_thread.syncs);
    cnd_signal(&queue->sync_thread.cond);
diff --git a/src/venus/vkr_queue.h b/src/venus/vkr_queue.h
index 5246a5a..9c802ad 100644
--- a/src/venus/vkr_queue.h
+++ b/src/venus/vkr_queue.h
@@ -95,10 +95,14 @@ vkr_queue_destroy(struct vkr_context *ctx, struct vkr_queue *queue);
 int
 vkr_queue_export_fence_sync_file(struct vkr_queue *queue, uint64_t fence_id, int *out_fd);
 
+/* timeline_point != 0 also materializes the fence into the context's shared
+ * timeline syncobj at that point (see vkr_context_timeline_import)
+ */
 bool
 vkr_queue_sync_submit(struct vkr_queue *queue,
                       uint32_t flags,
                       uint32_t ring_idx,
-                      uint64_t fence_id);
+                      uint64_t fence_id,
+                      uint64_t timeline_point);
 
 #endif /* VKR_QUEUE_H */
diff --git a/src/venus/vkr_renderer.c b/src/venus/vkr_renderer.c
index d7eaab3..704fe91 100644
--- a/src/venus/vkr_renderer.c
+++ b/src/venus/vkr_renderer.c
@@ -183,6 +183,53 @@ vkr_renderer_submit_fence(uint32_t ctx_id,
    return vkr_context_submit_fence(ctx, flags, ring_idx, fence_id);
 }
 
+bool
+vkr_renderer_submit_fence2(uint32_t ctx_id,
+                           uint32_t flags,
+                           uint64_t ring_idx,
+                           uint64_t fence_id,
+                           uint64_t timeline_point)
+{
+   TRACE_FUNC();
+
+   struct vkr_context *ctx = vkr_renderer_lookup_context(ctx_id);
+   if (!ctx)
+      return false;
+
+   assert(vkr_state.cbs->retire_fence);
+   return vkr_context_submit_fence2(ctx, flags, ring_idx, fence_id, timeline_point);
+}
+
+int
+vkr_renderer_get_timeline_fd(uint32_t ctx_id, int *out_syncobj_fd, int *out_node_fd)
+{
+   TRACE_FUNC();
+
+   struct vkr_context *ctx = vkr_renderer_lookup_context(ctx_id);
+   if (!ctx)
+      return -EINVAL;
+
+   return vkr_context_get_timeline_fd(ctx, out_syncobj_fd, out_node_fd);
+}
+
+int
+vkr_renderer_import_semaphore_sync_fd(uint32_t ctx_id,
+                                      uint64_t device_id,
+                                      uint64_t semaphore_id,
+                                      int sync_file_fd)
+{
+   TRACE_FUNC();
+
+   struct vkr_context *ctx = vkr_renderer_lookup_context(ctx_id);
+   if (!ctx) {
+      close(sync_file_fd);
+      return -EINVAL;
+   }
+
+   return vkr_context_import_semaphore_sync_fd(ctx, device_id, semaphore_id,
+                                               sync_file_fd);
+}
+
 bool
 vkr_renderer_create_resource(uint32_t ctx_id,
                              uint32_t res_id,
diff --git a/src/venus/vkr_renderer.h b/src/venus/vkr_renderer.h
index b606ce6..5ff81b2 100644
--- a/src/venus/vkr_renderer.h
+++ b/src/venus/vkr_renderer.h
@@ -57,6 +57,29 @@ vkr_renderer_submit_fence(uint32_t ctx_id,
                           uint64_t ring_idx,
                           uint64_t fence_id);
 
+/* like vkr_renderer_submit_fence; timeline_point != 0 also materializes the
+ * fence into the context's shared timeline syncobj at that point
+ */
+bool
+vkr_renderer_submit_fence2(uint32_t ctx_id,
+                           uint32_t flags,
+                           uint64_t ring_idx,
+                           uint64_t fence_id,
+                           uint64_t timeline_point);
+
+/* returns 0 and caller-owned fds (timeline syncobj + render node), or
+ * negative errno
+ */
+int
+vkr_renderer_get_timeline_fd(uint32_t ctx_id, int *out_syncobj_fd, int *out_node_fd);
+
+/* consumes sync_file_fd (driver owns it on success, closed on failure) */
+int
+vkr_renderer_import_semaphore_sync_fd(uint32_t ctx_id,
+                                      uint64_t device_id,
+                                      uint64_t semaphore_id,
+                                      int sync_file_fd);
+
 bool
 vkr_renderer_create_resource(uint32_t ctx_id,
                              uint32_t res_id,
diff --git a/src/virgl_context.h b/src/virgl_context.h
index 5db5cc3..7397b97 100644
--- a/src/virgl_context.h
+++ b/src/virgl_context.h
@@ -140,6 +140,34 @@ struct virgl_context {
                        uint64_t fence_id,
                        int *out_fd);
 
+   /* Like submit_fence, but additionally materialize the fence into the
+    * context's shared DRM timeline syncobj at the given point (see
+    * get_timeline_fd).  Optional.
+    */
+   int (*submit_fence2)(struct virgl_context *ctx,
+                        uint32_t flags,
+                        uint32_t ring_idx,
+                        uint64_t fence_id,
+                        uint64_t timeline_point);
+
+   /* Return fds of the context's DRM timeline syncobj and of a render node
+    * usable for syncobj ioctls on it, creating the timeline on first use.
+    * The caller owns the returned fds.  Points are materialized by
+    * submit_fence2.  Optional.
+    */
+   int (*get_timeline_fd)(struct virgl_context *ctx,
+                          int *out_syncobj_fd,
+                          int *out_node_fd);
+
+   /* Import a sync_file fd as the TEMPORARY payload of a venus binary
+    * semaphore identified by (device_id, semaphore_id) venus object ids, so
+    * queue submissions wait it on the GPU.  Consumes the fd.  Optional.
+    */
+   int (*import_semaphore_fd)(struct virgl_context *ctx,
+                              uint64_t device_id,
+                              uint64_t semaphore_id,
+                              int sync_file_fd);
+
    /* For DRM native contexts, return the device fd: */
    int (*get_device_fd)(struct virgl_context *ctx);
 
diff --git a/src/virglrenderer.c b/src/virglrenderer.c
index c82b916..cef4107 100644
--- a/src/virglrenderer.c
+++ b/src/virglrenderer.c
@@ -449,6 +449,54 @@ int virgl_renderer_context_export_fence(uint32_t ctx_id,
    return ctx->export_fence(ctx, ring_idx, fence_id, fd);
 }
 
+int virgl_renderer_context_create_fence2(uint32_t ctx_id,
+                                         uint32_t flags,
+                                         uint32_t ring_idx,
+                                         uint64_t fence_id,
+                                         uint64_t timeline_point)
+{
+   TRACE_FUNC();
+   struct virgl_context *ctx = virgl_context_lookup(ctx_id);
+   if (!ctx)
+      return -EINVAL;
+   if (!ctx->submit_fence2)
+      return -ENOTSUP;
+
+   assert(state.cbs->version >= 3 && state.cbs->write_context_fence);
+   return ctx->submit_fence2(ctx, flags, ring_idx, fence_id, timeline_point);
+}
+
+int virgl_renderer_context_get_timeline_fd(uint32_t ctx_id,
+                                           int *syncobj_fd,
+                                           int *node_fd)
+{
+   TRACE_FUNC();
+   struct virgl_context *ctx = virgl_context_lookup(ctx_id);
+   if (!ctx)
+      return -EINVAL;
+   if (!ctx->get_timeline_fd)
+      return -ENOTSUP;
+   return ctx->get_timeline_fd(ctx, syncobj_fd, node_fd);
+}
+
+int virgl_renderer_context_import_semaphore_fd(uint32_t ctx_id,
+                                               uint64_t device_id,
+                                               uint64_t semaphore_id,
+                                               int sync_file_fd)
+{
+   TRACE_FUNC();
+   struct virgl_context *ctx = virgl_context_lookup(ctx_id);
+   if (!ctx) {
+      close(sync_file_fd);
+      return -EINVAL;
+   }
+   if (!ctx->import_semaphore_fd) {
+      close(sync_file_fd);
+      return -ENOTSUP;
+   }
+   return ctx->import_semaphore_fd(ctx, device_id, semaphore_id, sync_file_fd);
+}
+
 void virgl_renderer_context_poll(uint32_t ctx_id)
 {
    TRACE_FUNC();
diff --git a/src/virglrenderer.h b/src/virglrenderer.h
index f59dd9a..6d36e31 100644
--- a/src/virglrenderer.h
+++ b/src/virglrenderer.h
@@ -467,6 +467,40 @@ VIRGL_EXPORT int virgl_renderer_context_export_fence(uint32_t ctx_id,
                                                      uint64_t fence_id,
                                                      int *fd);
 
+/*
+ * Like virgl_renderer_context_create_fence, but additionally materialize the
+ * fence into the context's shared DRM timeline syncobj at timeline_point
+ * (venus contexts only).  Callers obtain the timeline via
+ * virgl_renderer_context_get_timeline_fd and share it with the client, which
+ * can then export per-point sync_files locally without further IPC.
+ */
+VIRGL_EXPORT int virgl_renderer_context_create_fence2(uint32_t ctx_id,
+                                                      uint32_t flags,
+                                                      uint32_t ring_idx,
+                                                      uint64_t fence_id,
+                                                      uint64_t timeline_point);
+
+/*
+ * Return fds of the context's DRM timeline syncobj and of a render node
+ * usable for syncobj ioctls on it, creating the timeline on first use (venus
+ * contexts only).  The caller owns both fds.  Returns 0 on success, negative
+ * on error.
+ */
+VIRGL_EXPORT int virgl_renderer_context_get_timeline_fd(uint32_t ctx_id,
+                                                        int *syncobj_fd,
+                                                        int *node_fd);
+
+/*
+ * Import a sync_file fd as the TEMPORARY payload of a venus binary semaphore
+ * identified by (device_id, semaphore_id) venus object ids, so subsequent
+ * queue submissions wait it on the GPU instead of the guest CPU-waiting the
+ * fd (venus contexts only).  Consumes the fd.  Returns 0 on success.
+ */
+VIRGL_EXPORT int virgl_renderer_context_import_semaphore_fd(uint32_t ctx_id,
+                                                            uint64_t device_id,
+                                                            uint64_t semaphore_id,
+                                                            int sync_file_fd);
+
 VIRGL_EXPORT void virgl_renderer_context_poll(uint32_t ctx_id); /* force fences */
 VIRGL_EXPORT int virgl_renderer_context_get_poll_fd(uint32_t ctx_id);
 
diff --git a/vtest/meson.build b/vtest/meson.build
index c2bd278..387084f 100644
--- a/vtest/meson.build
+++ b/vtest/meson.build
@@ -29,6 +29,8 @@ vtest_sources = [
    'vtest_shm.h',
    'vtest_server.c',
    'vtest_renderer.c',
+   'vtest_gpu_alloc.c',
+   'vtest_gpu_alloc.h',
    'vtest_protocol.h',
    'vtest.h'
 ]
diff --git a/vtest/vtest.h b/vtest/vtest.h
index fa74075..5969851 100644
--- a/vtest/vtest.h
+++ b/vtest/vtest.h
@@ -56,6 +56,14 @@ void vtest_poll_context(struct vtest_context *ctx);
 int vtest_get_context_poll_fd(struct vtest_context *ctx);
 
 void vtest_set_current_context(struct vtest_context *ctx);
+const char *vtest_get_current_context_name(void);
+
+/* VTEST_STATS instrumentation (vtest_server.c) */
+void vtest_stats_count_import(bool query_only);
+
+int vtest_submit_cmd2_timeline(uint32_t length_dw);
+int vtest_timeline_get(uint32_t length_dw);
+int vtest_semaphore_import_sync_fd(uint32_t length_dw);
 
 int vtest_send_caps(uint32_t length_dw);
 int vtest_send_caps2(uint32_t length_dw);
@@ -96,6 +104,7 @@ int vtest_sync_write(uint32_t length_dw);
 int vtest_sync_wait(uint32_t length_dw);
 int vtest_sync_export_sync_file(uint32_t length_dw);
 int vtest_resource_import_blob(uint32_t length_dw);
+int vtest_resource_alloc_gpu(uint32_t length_dw);
 
 int vtest_submit_cmd2(uint32_t length_dw);
 
diff --git a/vtest/vtest_protocol.h b/vtest/vtest_protocol.h
index 3a7381f..95b0b0d 100644
--- a/vtest/vtest_protocol.h
+++ b/vtest/vtest_protocol.h
@@ -98,6 +98,22 @@
 #define VCMD_SYNC_EXPORT_SYNC_FILE 39
 /* import a dmabuf fd as a blob resource attached to the context */
 #define VCMD_RESOURCE_IMPORT_BLOB 40
+/* allocate a gralloc buffer on the host render GPU, reply with its dma_buf */
+#define VCMD_RESOURCE_ALLOC_GPU 41
+/* get the context's shared DRM timeline syncobj (reply: status + 1 fd) */
+#define VCMD_TIMELINE_GET 42
+/* submit_cmd2 whose fence also materializes a timeline point:
+ * payload = {point_lo, point_hi} followed by a regular submit_cmd2 payload;
+ * the point applies to the last fence-creating batch
+ */
+#define VCMD_SUBMIT_CMD2_TIMELINE 43
+/* import a sync_file fd (via SCM_RIGHTS) as the temporary payload of a venus
+ * semaphore, so the host GPU waits it instead of the guest CPU:
+ * payload = {device_id_lo, device_id_hi, semaphore_id_lo, semaphore_id_hi};
+ * reply = {status} — the roundtrip orders the import before any subsequent
+ * ring submission that waits on the semaphore
+ */
+#define VCMD_SEMAPHORE_IMPORT_SYNC_FD 44
 
 #define VCMD_RES_CREATE_SIZE 10
 #define VCMD_RES_CREATE_RES_HANDLE 0 /* must be 0 since protocol version 3 */
@@ -170,6 +186,10 @@ enum vcmd_param  {
    VCMD_PARAM_HAS_TIMELINE_SYNCOBJ = 2,
    /* server supports VCMD_SYNC_EXPORT_SYNC_FILE for venus sync points */
    VCMD_PARAM_HAS_VENUS_SYNC_FD = 3,
+   /* server supports VCMD_TIMELINE_GET + VCMD_SUBMIT_CMD2_TIMELINE */
+   VCMD_PARAM_HAS_TIMELINE_FENCE = 4,
+   /* server supports VCMD_SEMAPHORE_IMPORT_SYNC_FD */
+   VCMD_PARAM_HAS_SEMAPHORE_IMPORT = 5,
 };
 #define VCMD_GET_PARAM_SIZE 1
 #define VCMD_GET_PARAM_PARAM 0
@@ -262,6 +282,30 @@ enum vcmd_sync_export_sync_file_status {
 #define VCMD_RESOURCE_IMPORT_BLOB_SIZE_HI 1
 /* followed by 1 fd; resp res_id (0 = failure) */
 
+/* Allocate a gralloc buffer on the host render GPU (or host shmem for
+ * CPU-mappable buffers) and return its dma_buf.  Used by the guest gralloc
+ * so that window buffers are native to the compositing GPU.  The response is
+ * 7 dwords (status, stride, map_stride, modifier lo/hi, size lo/hi) followed
+ * by 1 fd (SCM_RIGHTS, server -> client) if status == OK.
+ */
+#define VCMD_RESOURCE_ALLOC_GPU_SIZE 4
+#define VCMD_RESOURCE_ALLOC_GPU_WIDTH 0
+#define VCMD_RESOURCE_ALLOC_GPU_HEIGHT 1
+#define VCMD_RESOURCE_ALLOC_GPU_FORMAT 2 /* DRM fourcc */
+#define VCMD_RESOURCE_ALLOC_GPU_FLAGS 3
+
+#define VCMD_ALLOC_GPU_FLAG_MAPPABLE (1u << 0) /* guest needs CPU mmap */
+#define VCMD_ALLOC_GPU_FLAG_SCANOUT (1u << 1)
+
+#define VCMD_RESOURCE_ALLOC_GPU_RESP_SIZE 7
+#define VCMD_RESOURCE_ALLOC_GPU_RESP_STATUS 0 /* 0 = OK */
+#define VCMD_RESOURCE_ALLOC_GPU_RESP_STRIDE 1
+#define VCMD_RESOURCE_ALLOC_GPU_RESP_MAP_STRIDE 2
+#define VCMD_RESOURCE_ALLOC_GPU_RESP_MODIFIER_LO 3
+#define VCMD_RESOURCE_ALLOC_GPU_RESP_MODIFIER_HI 4
+#define VCMD_RESOURCE_ALLOC_GPU_RESP_SIZE_LO 5
+#define VCMD_RESOURCE_ALLOC_GPU_RESP_SIZE_HI 6
+
 enum vcmd_submit_cmd2_flag {
    VCMD_SUBMIT_CMD2_FLAG_RING_IDX = 1 << 0,
    VCMD_SUBMIT_CMD2_FLAG_IN_FENCE_FD = 1 << 1,
diff --git a/vtest/vtest_renderer.c b/vtest/vtest_renderer.c
index f3212ad..3a9cbbb 100644
--- a/vtest/vtest_renderer.c
+++ b/vtest/vtest_renderer.c
@@ -53,6 +53,7 @@
 
 #include "vtest.h"
 #include "vtest_shm.h"
+#include "vtest_gpu_alloc.h"
 #include "vtest_protocol.h"
 #include "threadpool.h"
 
@@ -865,6 +866,13 @@ static struct vtest_context *vtest_get_current_context(void)
    return renderer.current_context;
 }
 
+const char *vtest_get_current_context_name(void)
+{
+   const struct vtest_context *ctx = vtest_get_current_context();
+
+   return ctx && ctx->debug_name ? ctx->debug_name : "unknown";
+}
+
 int vtest_ping_protocol_version(UNUSED uint32_t length_dw)
 {
    struct vtest_context *ctx = vtest_get_current_context();
@@ -970,6 +978,20 @@ int vtest_get_param(UNUSED uint32_t length_dw)
       resp[0] = true;
       resp[1] = !!(renderer.ctx_flags & VIRGL_RENDERER_VENUS);
       break;
+   case VCMD_PARAM_HAS_TIMELINE_FENCE:
+      /* the client falls back to VCMD_SYNC_EXPORT_SYNC_FILE if
+       * VCMD_TIMELINE_GET fails at runtime (e.g. no capable render node);
+       * VTEST_NO_TIMELINE force-disables for bisection
+       */
+      resp[0] = true;
+      resp[1] = !!(renderer.ctx_flags & VIRGL_RENDERER_VENUS) &&
+                !getenv("VTEST_NO_TIMELINE");
+      break;
+   case VCMD_PARAM_HAS_SEMAPHORE_IMPORT:
+      resp[0] = true;
+      resp[1] = !!(renderer.ctx_flags & VIRGL_RENDERER_VENUS) &&
+                !getenv("VTEST_NO_SEMAPHORE_IMPORT");
+      break;
    case VCMD_PARAM_HAS_TIMELINE_SYNCOBJ: {
 #ifdef ENABLE_DRM
       int fd = virgl_renderer_get_dev_fd(ctx->ctx_id);
@@ -2238,7 +2260,8 @@ struct drm_virtgpu_execbuffer_syncobj {
 static int vtest_submit_cmd2_batch(struct vtest_context *ctx,
                                    const struct vcmd_submit_cmd2_batch *batch,
                                    const uint32_t *cmds,
-                                   const uint32_t *syncs)
+                                   const uint32_t *syncs,
+                                   uint64_t timeline_point)
 {
    struct vtest_timeline_submit *submit = NULL;
    struct drm_virtgpu_execbuffer_syncobj *in_syncobj = NULL, *out_syncobj = NULL;
@@ -2361,10 +2384,18 @@ static int vtest_submit_cmd2_batch(struct vtest_context *ctx,
       uint64_t fence_id = (uintptr_t)submit;
 
       submit->timeline = timeline;
-      ret = virgl_renderer_context_create_fence(ctx->ctx_id,
-                                                VIRGL_RENDERER_FENCE_FLAG_MERGEABLE,
-                                                batch->ring_idx,
-                                                fence_id);
+      if (timeline_point) {
+         ret = virgl_renderer_context_create_fence2(ctx->ctx_id,
+                                                    VIRGL_RENDERER_FENCE_FLAG_MERGEABLE,
+                                                    batch->ring_idx,
+                                                    fence_id,
+                                                    timeline_point);
+      } else {
+         ret = virgl_renderer_context_create_fence(ctx->ctx_id,
+                                                   VIRGL_RENDERER_FENCE_FLAG_MERGEABLE,
+                                                   batch->ring_idx,
+                                                   fence_id);
+      }
       if (ret) {
          vtest_free_timeline_submit(submit);
          goto out;
@@ -2439,6 +2470,7 @@ int vtest_resource_import_blob(uint32_t length_dw)
    /* venus imports with size 0 when only querying memory properties (see
     * vn_get_memory_dma_buf_properties); derive the size from the dmabuf
     */
+   vtest_stats_count_import(size == 0);
    if (!size) {
       const off_t seek_size = lseek(fd, 0, SEEK_END);
       if (seek_size < 0) {
@@ -2491,6 +2523,64 @@ int vtest_resource_import_blob(uint32_t length_dw)
    return vtest_block_write(ctx->out_fd, resp_buf, sizeof(resp_buf)) >= 0 ? 0 : -1;
 }
 
+int vtest_resource_alloc_gpu(uint32_t length_dw)
+{
+   struct vtest_context *ctx = vtest_get_current_context();
+   uint32_t req[VCMD_RESOURCE_ALLOC_GPU_SIZE];
+   uint32_t resp_buf[VTEST_HDR_SIZE + VCMD_RESOURCE_ALLOC_GPU_RESP_SIZE];
+   uint32_t *resp = &resp_buf[VTEST_CMD_DATA_START];
+   int fd = -1;
+   int ret;
+
+   if (length_dw != VCMD_RESOURCE_ALLOC_GPU_SIZE)
+      return -EINVAL;
+
+   ret = ctx->input->read(ctx->input, req, sizeof(req));
+   if (ret != sizeof(req))
+      return -1;
+
+   const uint32_t width = req[VCMD_RESOURCE_ALLOC_GPU_WIDTH];
+   const uint32_t height = req[VCMD_RESOURCE_ALLOC_GPU_HEIGHT];
+   const uint32_t drm_format = req[VCMD_RESOURCE_ALLOC_GPU_FORMAT];
+   const uint32_t flags = req[VCMD_RESOURCE_ALLOC_GPU_FLAGS];
+
+   uint32_t stride = 0;
+   uint64_t modifier = 0;
+   uint64_t size = 0;
+
+   if (!width || !height || width > 32768 || height > 32768) {
+      ret = -EINVAL;
+   } else if (flags & VCMD_ALLOC_GPU_FLAG_MAPPABLE) {
+      ret = vtest_gpu_alloc_cpu(width, height, drm_format, &stride, &size, &fd);
+   } else {
+      ret = vtest_gpu_alloc_gpu(width, height, drm_format, &stride, &modifier,
+                                &size, &fd);
+   }
+   if (ret)
+      report_failed_call("vtest_gpu_alloc", ret);
+
+   memset(resp_buf, 0, sizeof(resp_buf));
+   resp_buf[VTEST_CMD_LEN] = VCMD_RESOURCE_ALLOC_GPU_RESP_SIZE;
+   resp_buf[VTEST_CMD_ID] = VCMD_RESOURCE_ALLOC_GPU;
+   resp[VCMD_RESOURCE_ALLOC_GPU_RESP_STATUS] = ret ? (uint32_t)-ret : 0;
+   resp[VCMD_RESOURCE_ALLOC_GPU_RESP_STRIDE] = stride;
+   /* both paths allocate linear, so the CPU view stride == GPU stride */
+   resp[VCMD_RESOURCE_ALLOC_GPU_RESP_MAP_STRIDE] = stride;
+   resp[VCMD_RESOURCE_ALLOC_GPU_RESP_MODIFIER_LO] = (uint32_t)modifier;
+   resp[VCMD_RESOURCE_ALLOC_GPU_RESP_MODIFIER_HI] = (uint32_t)(modifier >> 32);
+   resp[VCMD_RESOURCE_ALLOC_GPU_RESP_SIZE_LO] = (uint32_t)size;
+   resp[VCMD_RESOURCE_ALLOC_GPU_RESP_SIZE_HI] = (uint32_t)(size >> 32);
+
+   ret = vtest_block_write(ctx->out_fd, resp_buf, sizeof(resp_buf));
+   if (ret >= 0 && fd >= 0)
+      ret = vtest_send_fd(ctx->out_fd, fd);
+
+   if (fd >= 0)
+      close(fd);
+
+   return ret >= 0 ? 0 : ret;
+}
+
 int vtest_sync_export_sync_file(uint32_t length_dw)
 {
    struct vtest_context *ctx = vtest_get_current_context();
@@ -2560,7 +2650,7 @@ int vtest_sync_export_sync_file(uint32_t length_dw)
    return ret >= 0 ? 0 : ret;
 }
 
-int vtest_submit_cmd2(uint32_t length_dw)
+static int vtest_submit_cmd2_common(uint32_t length_dw, uint64_t timeline_point)
 {
    struct vtest_context *ctx = vtest_get_current_context();
    uint32_t *submit_cmd2_buf;
@@ -2611,7 +2701,9 @@ int vtest_submit_cmd2(uint32_t length_dw)
          return -EINVAL;
       }
 
-      ret = vtest_submit_cmd2_batch(ctx, &batch, cmds, syncs);
+      /* the timeline point applies to the last fence-creating batch */
+      const uint64_t point = (i == batch_count - 1) ? timeline_point : 0;
+      ret = vtest_submit_cmd2_batch(ctx, &batch, cmds, syncs, point);
       if (ret) {
          free(submit_cmd2_buf);
          return ret;
@@ -2623,6 +2715,92 @@ int vtest_submit_cmd2(uint32_t length_dw)
    return 0;
 }
 
+int vtest_submit_cmd2(uint32_t length_dw)
+{
+   return vtest_submit_cmd2_common(length_dw, 0);
+}
+
+int vtest_submit_cmd2_timeline(uint32_t length_dw)
+{
+   struct vtest_context *ctx = vtest_get_current_context();
+   uint32_t point_buf[2];
+   int ret;
+
+   if (length_dw < 2)
+      return -EINVAL;
+
+   ret = ctx->input->read(ctx->input, point_buf, sizeof(point_buf));
+   if (ret != sizeof(point_buf))
+      return -1;
+
+   const uint64_t point = point_buf[0] | (uint64_t)point_buf[1] << 32;
+   if (!point)
+      return -EINVAL;
+
+   return vtest_submit_cmd2_common(length_dw - 2, point);
+}
+
+int vtest_semaphore_import_sync_fd(uint32_t length_dw)
+{
+   struct vtest_context *ctx = vtest_get_current_context();
+   uint32_t cmd_buf[4];
+   uint32_t resp_buf[VTEST_HDR_SIZE + 1];
+   int ret;
+
+   if (length_dw != ARRAY_SIZE(cmd_buf))
+      return -EINVAL;
+
+   ret = ctx->input->read(ctx->input, cmd_buf, sizeof(cmd_buf));
+   if (ret != sizeof(cmd_buf))
+      return -1;
+
+   const uint64_t device_id = cmd_buf[0] | (uint64_t)cmd_buf[1] << 32;
+   const uint64_t semaphore_id = cmd_buf[2] | (uint64_t)cmd_buf[3] << 32;
+
+   int fd = vtest_receive_fd(ctx->input->data.fd);
+   if (fd < 0)
+      return report_failed_call("vtest_receive_fd", fd);
+
+   /* consumes fd */
+   ret = virgl_renderer_context_import_semaphore_fd(ctx->ctx_id, device_id,
+                                                    semaphore_id, fd);
+
+   resp_buf[VTEST_CMD_LEN] = 1;
+   resp_buf[VTEST_CMD_ID] = VCMD_SEMAPHORE_IMPORT_SYNC_FD;
+   resp_buf[VTEST_CMD_DATA_START] = ret ? 1 : 0;
+   ret = vtest_block_write(ctx->out_fd, resp_buf, sizeof(resp_buf));
+
+   return ret >= 0 ? 0 : ret;
+}
+
+int vtest_timeline_get(UNUSED uint32_t length_dw)
+{
+   struct vtest_context *ctx = vtest_get_current_context();
+   uint32_t resp_buf[VTEST_HDR_SIZE + 1];
+   int fds[2] = { -1, -1 };
+   int ret;
+
+   /* on success two fds follow the reply: timeline syncobj, render node */
+   ret = virgl_renderer_context_get_timeline_fd(ctx->ctx_id, &fds[0], &fds[1]);
+
+   resp_buf[VTEST_CMD_LEN] = 1;
+   resp_buf[VTEST_CMD_ID] = VCMD_TIMELINE_GET;
+   resp_buf[VTEST_CMD_DATA_START] = ret ? 1 : 0;
+   ret = vtest_block_write(ctx->out_fd, resp_buf, sizeof(resp_buf));
+   if (ret >= 0 && fds[0] >= 0) {
+      ret = vtest_send_fd(ctx->out_fd, fds[0]);
+      if (ret >= 0)
+         ret = vtest_send_fd(ctx->out_fd, fds[1]);
+   }
+
+   for (int i = 0; i < 2; i++) {
+      if (fds[i] >= 0)
+         close(fds[i]);
+   }
+
+   return ret >= 0 ? 0 : ret;
+}
+
 #ifdef ENABLE_DRM
 
 int vtest_drm_sync_create(UNUSED uint32_t length_dw)
diff --git a/vtest/vtest_server.c b/vtest/vtest_server.c
index dcf1b19..94584fe 100644
--- a/vtest/vtest_server.c
+++ b/vtest/vtest_server.c
@@ -33,12 +33,16 @@
 #include <stdlib.h>
 #include <sys/types.h>
 #include <sys/socket.h>
+#include <sys/stat.h>
 #include <netinet/in.h>
 #include <sys/un.h>
 #include <fcntl.h>
 #include <getopt.h>
 #include <string.h>
 
+#include <inttypes.h>
+#include <time.h>
+
 #include "util.h"
 #include "util/list.h"
 #include "util/u_math.h"
@@ -52,6 +56,40 @@
 #include <sys/select.h>
 #endif
 
+/* VTEST_STATS=<seconds>: periodically log per-command dispatch counts and
+ * times (and context poll wakeups) to stderr, per client process.  Costs two
+ * clock_gettime per dispatched command when enabled, nothing when disabled.
+ */
+#define VTEST_STATS_MAX_CMDS 64
+static struct {
+   int period;        /* seconds; 0 = off, -1 = env not checked yet */
+   int64_t next_dump;
+   uint64_t count[VTEST_STATS_MAX_CMDS];
+   uint64_t ns[VTEST_STATS_MAX_CMDS];
+   uint64_t polls;
+} vtest_stats = { .period = -1 };
+
+static int64_t vtest_stats_now(void)
+{
+   struct timespec ts;
+   clock_gettime(CLOCK_MONOTONIC, &ts);
+   return (int64_t)ts.tv_sec * 1000000000 + ts.tv_nsec;
+}
+
+static uint64_t vtest_stats_imports_query;
+static uint64_t vtest_stats_imports_real;
+
+/* called from vtest_resource_import_blob to split property-query imports
+ * (size == 0) from real buffer imports in the stats
+ */
+void vtest_stats_count_import(bool query_only)
+{
+   if (query_only)
+      vtest_stats_imports_query++;
+   else
+      vtest_stats_imports_real++;
+}
+
 enum vtest_client_result {
    VTEST_CLIENT_DISCONNECTED = 1,
    VTEST_CLIENT_ERROR_INPUT_READ,
@@ -426,6 +464,11 @@ static void vtest_server_open_socket(void)
       goto err;
    }
 
+   /* guest clients run as arbitrary uids inside the container; make the
+    * socket world-connectable here (an external chmod races with the
+    * unlink+bind above) */
+   chmod(un.sun_path, 0777);
+
    return;
 
 err:
@@ -525,6 +568,7 @@ static void vtest_server_dispatch_clients(void)
       int ret;
 
       if (client->context_need_poll) {
+         vtest_stats.polls++;
          vtest_poll_context(client->context);
          client->context_need_poll = false;
       }
@@ -732,8 +776,37 @@ static const struct vtest_command {
    HANDLER(RESOURCE_EXPORT_FD,          resource_export_fd,        true   ),
    HANDLER(SYNC_EXPORT_SYNC_FILE,       sync_export_sync_file,     true   ),
    HANDLER(RESOURCE_IMPORT_BLOB,        resource_import_blob,      true   ),
+   HANDLER(RESOURCE_ALLOC_GPU,          resource_alloc_gpu,        false  ),
+   HANDLER(TIMELINE_GET,                timeline_get,              true   ),
+   HANDLER(SUBMIT_CMD2_TIMELINE,        submit_cmd2_timeline,      true   ),
+   HANDLER(SEMAPHORE_IMPORT_SYNC_FD,    semaphore_import_sync_fd,  true   ),
 };
 
+static void vtest_stats_dump(void)
+{
+   const char *context_name = vtest_get_current_context_name();
+
+   fprintf(stderr, "vtest stats [%s]: polls %" PRIu64 ", imports query/real %" PRIu64
+           "/%" PRIu64 "\n", context_name, vtest_stats.polls,
+           vtest_stats_imports_query, vtest_stats_imports_real);
+   vtest_stats.polls = 0;
+   vtest_stats_imports_query = 0;
+   vtest_stats_imports_real = 0;
+
+   for (uint32_t i = 0; i < ARRAY_SIZE(vtest_commands) && i < VTEST_STATS_MAX_CMDS; i++) {
+      if (!vtest_stats.count[i])
+         continue;
+      fprintf(stderr, "vtest stats [%s]:   %-26s %7" PRIu64 " calls  avg %5" PRIu64
+              " us  total %" PRIu64 " us\n",
+              context_name, vtest_commands[i].name ? vtest_commands[i].name : "?",
+              vtest_stats.count[i],
+              vtest_stats.ns[i] / vtest_stats.count[i] / 1000,
+              vtest_stats.ns[i] / 1000);
+      vtest_stats.count[i] = 0;
+      vtest_stats.ns[i] = 0;
+   }
+}
+
 static int vtest_client_dispatch_commands(struct vtest_client *client)
 {
    TRACE_FUNC();
@@ -766,6 +839,8 @@ static int vtest_client_dispatch_commands(struct vtest_client *client)
 
    vtest_poll_resource_busy_wait();
    if (header[1] <= 0 || header[1] >= ARRAY_SIZE(vtest_commands)) {
+      fprintf(stderr, "bad command id %u (len %u, table size %zu)\n",
+              header[1], header[0], (size_t)ARRAY_SIZE(vtest_commands));
       return VTEST_CLIENT_ERROR_COMMAND_ID;
    }
 
@@ -785,8 +860,30 @@ static int vtest_client_dispatch_commands(struct vtest_client *client)
 
    vtest_set_current_context(client->context);
 
+   if (vtest_stats.period < 0) {
+      const char *env = getenv("VTEST_STATS");
+      vtest_stats.period = env ? atoi(env) : 0;
+      if (vtest_stats.period > 0)
+         vtest_stats.next_dump =
+            vtest_stats_now() + (int64_t)vtest_stats.period * 1000000000;
+   }
+
    void *trace_scope = TRACE_SCOPE_BEGIN(cmd->name);
-   ret = cmd->dispatch(header[0]);
+   if (vtest_stats.period > 0) {
+      const int64_t t0 = vtest_stats_now();
+      ret = cmd->dispatch(header[0]);
+      const int64_t t1 = vtest_stats_now();
+      if (header[1] < VTEST_STATS_MAX_CMDS) {
+         vtest_stats.count[header[1]]++;
+         vtest_stats.ns[header[1]] += t1 - t0;
+      }
+      if (t1 >= vtest_stats.next_dump) {
+         vtest_stats_dump();
+         vtest_stats.next_dump = t1 + (int64_t)vtest_stats.period * 1000000000;
+      }
+   } else {
+      ret = cmd->dispatch(header[0]);
+   }
    TRACE_SCOPE_END(trace_scope);
 
    if (ret < 0) {
